name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r router/requirements.txt
          pip install pytest pytest-asyncio pytest-cov ruff pyyaml mypy

      - name: Lint with ruff
        run: ruff check router/app tests

      - name: Type-check with mypy
        run: mypy router/app --ignore-missing-imports || true

      - name: Run tests with coverage
        run: pytest tests/ --cov=router/app --cov-report=xml -v

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.xml