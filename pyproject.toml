[project]
name = "ai-unified"
version = "0.1.0"
description = "Unified AI utilities"
authors = [{name = "compactly8274", email = "youremail@example.com"}]
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"
